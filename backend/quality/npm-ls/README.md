# npm-ls — JavaScript / Node.js 24

Full dependency tree for the real `package.json`.

Run from the repo root:
```
npm ls --all --json
```

Verified in this session — real output: genuine tree with real
`extraneous` entries for transitive eslint/stryker tooling packages.
