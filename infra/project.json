{
  "name": "infra",
  "root": "infra",
  "projectType": "application",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "tflint --chdir=infra" }
    },
    "scan": {
      "executor": "nx:run-commands",
      "options": { "command": "checkov -d infra --compact" }
    }
  }
}
