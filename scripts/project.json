{
  "name": "scripts",
  "root": "scripts",
  "projectType": "application",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "shellcheck scripts/setup.sh" }
    },
    "format-check": {
      "executor": "nx:run-commands",
      "options": { "command": "shfmt -d scripts/setup.sh" }
    }
  }
}
